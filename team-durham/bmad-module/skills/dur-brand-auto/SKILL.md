---
name: dur-brand-auto
description: Run bounded Team Durham BMad brand auto mode.
---

# Team Durham Bounded Auto Mode

Run the existing `/auto` and `/brand-auto` contract:

```text
Scout ContextPacket → select specialist → one reversible non-shipping slice
→ explicit brand verification → compaction receipt → terminal state
```

Respect the 12,000-token default and five-iteration default. Stop for external publishing,
client delivery, locked-strategy changes, governance changes, asset deletion, or missing authority.
